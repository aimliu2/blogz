---
---
// retur HTML blog render
// depends on blog_limit params

export const blogRender = (category, title, date, description, url, cover_image) =>{
    return (`
        <div class="col-md-6">
            <div class="row g-0 border rounded overflow-hidden flex-md-row mb-4 shadow-sm h-md-250 position-relative">
                <div class="col p-4 d-flex flex-column position-static">
                    <strong class="d-inline-block mb-2 text-primary">${category}</strong>
                    <h3 class="mb-0">${title}</h3>
                    <div class="mb-1 text-muted">${date}</div>
                <p class="card-text mb-auto">${description}</p>
                <a href="${url}" class="stretched-link">Continue reading</a>
                </div>
            <div class="col-auto d-none d-lg-block">
            <img src="${cover_image}" style="width:200px;height:250px;object-fit:cover"></img>
            </div>
            </div>
        </div>
        `)
}
